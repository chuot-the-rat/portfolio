/**
 * Shared motion tokens for Framer Motion + CSS custom properties.
 * Keep these aligned with variables.css motion tokens.
 */

export const MOTION_DURATION = {
    hoverFast: 0.15,
    hoverBase: 0.22,
    hoverSlow: 0.25,
    revealBase: 0.5,
    revealSlow: 0.6,
    staggerFast: 0.06,
    staggerBase: 0.08,
    previewDelay: 0.12,
};

export const MOTION_EASE = {
    smooth: [0.25, 0, 0, 1],
    editorial: [0.16, 1, 0.3, 1],
};
